[build]
  command = "npm ci --legacy-peer-deps && npm run build"
  publish = "dist/sage-prototype/browser"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
